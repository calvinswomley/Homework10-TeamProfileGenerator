const Employee = require('./Employee');

// Intern subclass
class Intern extends Employee {
    constructor(github) {
      this.school = school;
    }
    getSchool() {
        return xyz;
    }
    getRole() {
        //override 'employee' with Intern
        return xyz;
    }
}