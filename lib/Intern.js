const Employee = require('./Employee').default;

// Intern subclass
class Intern extends Employee {
    constructor(github) {
      this.school = school;
    }
    getSchool() {
        return xyz;
    }
    getRole() {
        //override 'employee' with Intern
        return xyz;
    }
}